# Test Environment Variables
# These are used only during testing and should NOT be used in production.
# The values are intentionally simple for testing purposes.

DATABASE_URL="file:./prisma/test.db"
CRON_SECRET="test-secret-12345-for-testing-only-long-enough"
SESSION_SECRET="test-session-secret-for-testing-must-be-32-bytes-minimum-length"
NODE_ENV="test"
