Agent completed. agent_id: phase6c-planning, agent_type: Plan Mode - Strategic Planning & Architecture, status: completed, description: Phase 6C planning and breakdown, elapsed: 132s, total_turns: 0, duration: 127s

Perfect! Now I have comprehensive context. Let me create a detailed Phase 6C implementation plan.

---

# Phase 6C Implementation Plan: Claiming Cadence Feature

## Executive Summary

This plan orchestrates the implementation of **benefit claiming cadences** across the Card-Benefits platform. The feature enables the system to track how often users can claim benefits (MONTHLY, QUARTERLY, SEMI_ANNUAL, FLEXIBLE_ANNUAL, ONE_TIME) and enforce claiming limits based on each benefit's specific claiming pattern.

**Scope**: 87 benefits across 4+ premium credit cards  
**Affected Files**: 8 core files  
**Parallelizable Work Streams**: 3  
**Estimated Stories**: 18 total  
**High Priority Cadences**: MONTHLY (highest loss risk: $15-25/month expiration)

---

## Epic 1: Data Model & Database Foundation

### Feature 1.1: Schema & Type Definitions

#### Story 1.1.1: Add claiming cadence fields to Prisma schema
- **File**: `prisma/schema.prisma`
- **Changes**:
  - Add `claimingCadence: String` field to MasterBenefit model
  - Add `claimingAmount: Int` field (in cents) to MasterBenefit model
  - Add `claimingWindowEnd: String?` field for custom windows (e.g., "0918" for Sept 18)
  - Add indexes on claimingCadence for filtering
  - Add type validation comments
- **Acceptance Criteria**:
  - All 3 fields present in MasterBenefit model
  - Index on claimingCadence for query optimization
  - TypeScript types auto-generated by Prisma
  - Schema compiles without errors

#### Story 1.1.2: Create database migration file
- **File**: `prisma/migrations/[timestamp]_add_claiming_cadence_fields/migration.sql`
- **Changes**:
  - ALTER TABLE "MasterBenefit" ADD COLUMN "claimingCadence" VARCHAR(50)
  - ALTER TABLE "MasterBenefit" ADD COLUMN "claimingAmount" INTEGER DEFAULT 0
  - ALTER TABLE "MasterBenefit" ADD COLUMN "claimingWindowEnd" VARCHAR(10)
  - CREATE INDEX idx_masterbenefit_claimingcadence on "MasterBenefit"("claimingCadence")
  - Include migration rollback SQL
- **Acceptance Criteria**:
  - Migration file follows Prisma naming convention
  - All ADD COLUMN statements are non-breaking (nullable or default values)
  - Migration is reversible
  - Can be run against production database safely

#### Story 1.1.3: Update TypeScript type definitions
- **File**: `src/lib/benefit-period-utils.ts`
- **Changes**:
  - Add `ClaimingCadence` type: `'MONTHLY' | 'QUARTERLY' | 'SEMI_ANNUAL' | 'FLEXIBLE_ANNUAL' | 'ONE_TIME'`
  - Export from utils file
  - Add type guards for cadence validation
  - Create constants file: `src/lib/claiming-cadence-constants.ts` with:
    - CLAIMING_CADENCES enum
    - Cadence descriptions/labels
    - UI priority levels (CRITICAL/HIGH/MEDIUM/LOW)
- **Acceptance Criteria**:
  - New types exported and accessible in components
  - Type guards prevent invalid cadence values
  - Constants file organized by cadence type
  - Zero TypeScript errors

---

### Feature 1.2: Data Population Strategy

#### Story 1.2.1: Map claiming cadences for 68 premium benefits
- **File**: `scripts/seed-premium-cards.js`
- **Changes**:
  - Update all benefit objects to include `claimingCadence` and `claimingAmount`
  - Reference research findings for each benefit (see mapping below)
  - Handle Amex Platinum special case (Sept 18 split) with `claimingWindowEnd: "0918"`
  - Validate amounts are in cents (×100)
- **Affected Cards** (from research findings):
  - Chase Sapphire Reserve (4 benefits)
  - Chase Sapphire Preferred (6 benefits)
  - American Express Platinum (12 benefits including monthly, quarterly, semi-annual)
  - American Express Gold (5 benefits)
  - Capital One Venture X (3 benefits)
  - Delta SkyMiles Reserve (2 benefits)
  - And 36+ additional premium card benefits
- **Acceptance Criteria**:
  - All 68 premium benefits have claimingCadence populated
  - All 68 benefits have claimingAmount in cents
  - Amex Platinum uses "0918" for Sept 18 split benefits
  - Script validates data before seeding
  - Zero data integrity errors

**Data Mapping Reference** (from research):
| Benefit | Card | Cadence | Amount | Notes |
|---------|------|---------|--------|-------|
| Uber Credits | Amex Plat | MONTHLY | 1500 | $15/mo, lose if missed |
| Entertainment | Amex Plat | MONTHLY | 2500 | $25/mo, high loss risk |
| Hotel Credit H1 | Amex Plat | SEMI_ANNUAL | 20000 | $200, Jan 1-Sept 17 |
| Hotel Credit H2 | Amex Plat | SEMI_ANNUAL | 30000 | $300, Sept 18-Dec 31 |
| Dining Credit | Amex Plat | QUARTERLY | 10000 | $100/quarter |
| Airline Fee | Amex Plat | FLEXIBLE_ANNUAL | 20000 | $200, full year |
| Global Entry | Chase CSR | ONE_TIME | 10500 | $105, per 5 years |
| Travel Credit | Chase CSR | FLEXIBLE_ANNUAL | 30000 | $300/year |

#### Story 1.2.2: Map claiming cadences for 19 master catalog benefits
- **File**: `prisma/seed.ts`
- **Changes**:
  - Update MASTER_CARDS seed data with claimingCadence and claimingAmount
  - Reference 19 master benefits from Phase 6C research
  - Validate against MASTER_CARDS structure
- **Acceptance Criteria**:
  - All 19 master catalog benefits have claimingCadence
  - All 19 have claimingAmount in cents
  - Matches premium seed data for overlapping benefits
  - Script execution completes without errors

#### Story 1.2.3: Create comprehensive benefit mapping document
- **File**: `docs/PHASE6C-BENEFIT-CLAIMING-MAPPING.md`
- **Changes**:
  - Document all 87 benefits with their claiming cadence and amount
  - Organized by card issuer and cadence type
  - Include visual reference table
  - Special notes for edge cases (Amex Sept 18 split, varying monthly amounts)
- **Acceptance Criteria**:
  - All 87 benefits documented
  - Cross-reference with research findings
  - Ready for QA validation

---

## Epic 2: Utility Functions & Business Logic

### Feature 2.1: Benefit Period Calculation Logic

#### Story 2.1.1: Update calculateAmountPerPeriod() to use claimingAmount
- **File**: `src/lib/benefit-period-utils.ts`
- **Changes**:
  - Refactor `calculateAmountPerPeriod()` function to:
    - Accept benefit object with `claimingAmount` field
    - Return claimingAmount directly instead of calculating from stickerValue
    - Handle FLEXIBLE_ANNUAL cadence (return full claimingAmount for full year)
    - Handle MONTHLY cadence (return claimingAmount per month)
    - Handle QUARTERLY cadence (return claimingAmount per quarter)
    - Handle SEMI_ANNUAL cadence (return claimingAmount per half-year)
    - Handle ONE_TIME cadence (return claimingAmount once, then zero)
  - Update function signature with proper typing
- **Acceptance Criteria**:
  - Function returns correct amount for all 5 cadence types
  - Properly typed with TypeScript generics
  - Handles edge case: Amex Hotel ($200 H1, $300 H2)
  - No dependency on stickerValue anymore
  - Unit tests pass

#### Story 2.1.2: Create getClaimingWindowBoundaries() function
- **File**: `src/lib/benefit-period-utils.ts`
- **Changes**:
  - New function specifically for claiming cadences (distinct from resetCadence)
  - Handles Amex special case: Sept 18 split when `claimingWindowEnd: "0918"`
  - Returns period boundaries based on claimingCadence and claimingWindowEnd
  - Special logic:
    - If claimingWindowEnd is "0918" and cadence is SEMI_ANNUAL:
      - H1: Jan 1 - Sept 17
      - H2: Sept 18 - Dec 31
    - If claimingWindowEnd is "0918" and cadence is QUARTERLY:
      - Q1: Sept 18 - Sept 30
      - Q2: Oct 1 - Dec 31
      - (rest handled as calendar quarters)
  - For non-custom windows, use standard calendar periods
- **Acceptance Criteria**:
  - Correct boundaries for all cadence types
  - Amex Sept 18 split handled correctly
  - Tested with reference dates throughout year
  - Unit test coverage: 100% of branches

#### Story 2.1.3: Create getClaimingLimitForPeriod() function
- **File**: `src/lib/benefit-period-utils.ts`
- **Changes**:
  - New utility function to validate if user can claim more
  - Input: benefit with claimingCadence and claimingAmount, period date, usage records
  - Output: remaining claimable amount for period
  - Logic:
    - Get period boundaries using getClaimingWindowBoundaries()
    - Sum usage records within period
    - Return max(0, claimingAmount - totalUsedInPeriod)
    - For ONE_TIME: return 0 if ever used, else claimingAmount
  - Handle edge case: FLEXIBLE_ANNUAL (check full calendar year)
- **Acceptance Criteria**:
  - Correct remaining amount for all cadence types
  - ONE_TIME cadence returns 0 after first use
  - Handles partial claims correctly
  - Unit test coverage: 100%

---

### Feature 2.2: Claiming Validation Logic

#### Story 2.2.1: Create claimingValidation utilities module
- **File**: `src/lib/claiming-validation.ts`
- **Changes**:
  - Export validateClaimingAmount() function:
    - Accepts: benefit, claimAmount, usageRecords, referenceDate
    - Returns: { valid: boolean, error?: string, remainingAmount: number }
    - Validates claimAmount doesn't exceed period limit
    - Validates cadence-specific rules
  - Export isClaimingWindowOpen() function:
    - Accepts: benefit, referenceDate
    - Returns: boolean indicating if user can claim now
    - For MONTHLY: always true
    - For QUARTERLY/SEMI_ANNUAL: true if within window
    - For FLEXIBLE_ANNUAL: true if before Dec 31
    - For ONE_TIME: true if not yet used
  - Export daysUntilExpiration() function:
    - Accepts: benefit, referenceDate
    - Returns: number of days until period ends
    - Used for urgency indicators in UI
- **Acceptance Criteria**:
  - All validation rules implemented
  - Proper error messages for each failure case
  - Unit test coverage: 100%
  - No dependency on browser APIs

---

## Epic 3: Frontend Components & UI

### Feature 3.1: Benefit Display Components

#### Story 3.1.1: Update BenefitUsageProgress component for cadence awareness
- **File**: `src/components/benefits/BenefitUsageProgress.tsx`
- **Changes**:
  - Accept benefit object with new claimingCadence and claimingAmount fields
  - Update interface to include:
    - `benefit.claimingCadence`
    - `benefit.claimingAmount`
    - `benefit.claimingWindowEnd`
  - Render cadence-specific UI:
    - **MONTHLY**: Show "⏰ $X available THIS MONTH" + countdown badge "29 days left!"
    - **QUARTERLY**: Show "⏰ $X available THIS QUARTER" + window dates + countdown
    - **SEMI_ANNUAL**: Show "⏰ $X available H1/H2" + specific window dates
    - **FLEXIBLE_ANNUAL**: Show "✓ $X available anytime" + simple progress
    - **ONE_TIME**: Show "One-time credit: $X" + checkbox or badge
  - Add color-coded urgency indicators:
    - 🔴 RED: MONTHLY benefits (< 7 days left = critical)
    - 🟠 ORANGE: QUARTERLY/SEMI_ANNUAL (< 14 days = high)
    - 🟡 YELLOW: Within 30 days of any deadline
    - 🟢 GREEN: FLEXIBLE_ANNUAL or > 30 days remaining
  - Use daysUntilExpiration() to calculate urgency
- **Acceptance Criteria**:
  - All 5 cadence types render with appropriate messaging
  - Urgency colors display correctly
  - Progress bar works for FLEXIBLE_ANNUAL
  - Monthly/quarterly windows show with countdown
  - Responsive on mobile/tablet/desktop
  - Zero accessibility violations

#### Story 3.1.2: Create CadenceIndicator component
- **File**: `src/components/benefits/CadenceIndicator.tsx`
- **Changes**:
  - New reusable component showing cadence badge and urgency
  - Props: benefit, referenceDate
  - Renders:
    - Cadence label (e.g., "Monthly", "Quarterly")
    - Days remaining badge
    - Color-coded background
    - Tooltip on hover showing full details
  - Used by BenefitUsageProgress and MarkBenefitUsedModal
- **Acceptance Criteria**:
  - Component is standalone and reusable
  - Proper typing with TypeScript
  - Tooltip shows correct window information
  - Mobile-friendly
  - Zero console errors

#### Story 3.1.3: Create PeriodClainingHistory component
- **File**: `src/components/benefits/PeriodClaimingHistory.tsx`
- **Changes**:
  - New component showing past periods and their claim status
  - For MONTHLY: Show last 12 months with ✓/× indicator
  - For QUARTERLY: Show last 4 quarters
  - For SEMI_ANNUAL: Show current + previous half-years
  - For FLEXIBLE_ANNUAL: Show current year progress
  - For ONE_TIME: Show "Used on [date]" or "Available"
  - Display claimed amount and dates
  - Highlight periods where benefit was lost
- **Acceptance Criteria**:
  - Correct historical data display
  - Clear visual indication of missed periods
  - Matches research findings for loss tracking
  - Responsive layout
  - Unit tested with mock data

---

### Feature 3.2: Modal & Claiming Components

#### Story 3.2.1: Update MarkBenefitUsedModal for claiming limits
- **File**: `src/components/benefits/MarkBenefitUsedModal.tsx`
- **Changes**:
  - Accept benefit with claimingCadence and claimingAmount
  - Update validation logic:
    - Use getClaimingLimitForPeriod() to check remaining amount
    - Show error if user tries to claim over limit
    - Display current period's remaining amount prominently
  - Show cadence-specific UI:
    - Display period window (e.g., "March 1-31, 2026")
    - Show maximum claimable for this period
    - Show how much already claimed this period
    - For FLEXIBLE_ANNUAL: Show full-year total instead
  - Add warning for MONTHLY benefits:
    - "⚠️ Only 3 days left to claim this month's $15 credit"
  - Prevent claiming over period limit:
    - Max input field based on remaining amount
    - Show validation error with claim limits
  - Add smart suggestion based on cadence:
    - "This is a monthly benefit - it resets March 1st"
    - "This benefit expires Dec 31st"
- **Acceptance Criteria**:
  - Correctly calculates remaining claimable amount
  - Prevents over-claiming for all cadence types
  - Shows proper warning messages
  - Modal validation matches backend validation
  - Unit tests for all claiming scenarios

#### Story 3.2.2: Create ClaimingLimitInfo subcomponent
- **File**: `src/components/benefits/ClaimingLimitInfo.tsx`
- **Changes**:
  - Extracted subcomponent showing claiming limits
  - Props: benefit, currentPeriodUsed, remainingAmount, periodWindow
  - Renders:
    - Period information (dates and days remaining)
    - Amount used / Amount available
    - Visual progress bar
    - Warning if near/at limit
  - Used by MarkBenefitUsedModal
- **Acceptance Criteria**:
  - Component properly typed
  - Clear, accessible UI for limit display
  - Consistent styling with rest of app

---

## Epic 4: API & Backend Validation

### Feature 4.1: Benefits Usage Route Enhancement

#### Story 4.1.1: Add claiming amount validation to POST /api/benefits/usage
- **File**: `src/app/api/benefits/usage/route.ts`
- **Changes**:
  - Update POST handler to validate against claimingAmount
  - Before creating usage record:
    - Fetch benefit with claimingCadence and claimingAmount
    - Get usage records for the period (use getClaimingWindowBoundaries)
    - Calculate remaining claimable (use getClaimingLimitForPeriod)
    - Validate usageAmount doesn't exceed remaining
  - Return validation error with details:
    ```json
    {
      "success": false,
      "error": "CLAIMING_LIMIT_EXCEEDED",
      "message": "Cannot claim more than $X this month",
      "maxClaimable": 1500,
      "alreadyClaimed": 500,
      "periodEnd": "2026-04-30T23:59:59Z"
    }
    ```
  - Add specific error for ONE_TIME cadence:
    - "ALREADY_CLAIMED": "This one-time benefit has already been claimed"
- **Acceptance Criteria**:
  - Validates all 5 cadence types correctly
  - Prevents over-claiming
  - Returns clear error messages
  - Handles edge cases (leap years, Amex Sept 18 split)
  - Integration tests pass
  - No breaking changes to existing API

#### Story 4.1.2: Add claiming validation to GET /api/benefits/usage
- **File**: `src/app/api/benefits/usage/route.ts`
- **Changes**:
  - Update GET handler to include cadence info
  - Response should include for each usage record:
    - `claimingCadence`
    - `maxClaimableThisPeriod`
    - `alreadyClaimedThisPeriod`
    - `daysUntilExpiration`
  - Add optional filter by cadence: `?cadence=MONTHLY`
  - Add optional filter by urgency: `?urgency=CRITICAL` (only red-flag benefits)
- **Acceptance Criteria**:
  - GET response includes claiming metadata
  - Filters work correctly
  - Response is cacheable (ETag support)
  - Performance acceptable for 50+ benefits

#### Story 4.1.3: Create /api/benefits/claiming-limits endpoint
- **File**: `src/app/api/benefits/claiming-limits/route.ts`
- **Changes**:
  - New GET endpoint: `/api/benefits/claiming-limits?benefitId=[id]&date=[ISO]`
  - Returns claiming limit info for a benefit on specific date:
    ```json
    {
      "benefitId": "...",
      "claimingCadence": "MONTHLY",
      "periodStart": "2026-04-01T00:00:00Z",
      "periodEnd": "2026-04-30T23:59:59Z",
      "maxClaimableAmount": 1500,
      "alreadyClaimedAmount": 500,
      "remainingAmount": 1000,
      "daysUntilExpiration": 18,
      "isClaimingWindowOpen": true,
      "warningLevel": "HIGH"
    }
    ```
  - Used by frontend for real-time validation
  - Rate-limited (no auth required for now)
- **Acceptance Criteria**:
  - Endpoint works for all benefit types
  - Returns accurate claiming limits
  - Performance < 100ms response time
  - Handles edge cases (midnight boundaries, leap years)

---

## Epic 5: Data Migration & Testing

### Feature 5.1: Migration Execution

#### Story 5.1.1: Execute database migration
- **File**: `prisma/migrations/[timestamp]_add_claiming_cadence_fields/`
- **Changes**:
  - Run migration against development database first
  - Verify schema changes with `prisma db push`
  - Verify no data loss or errors
  - Generate Prisma client updates
- **Acceptance Criteria**:
  - Migration runs successfully
  - No data loss
  - All new fields are NULL or have defaults
  - Prisma client regenerated

#### Story 5.1.2: Seed database with claiming cadence data
- **File**: Manual execution of seed scripts
- **Changes**:
  - Execute `prisma/seed.ts` to populate 19 master benefits
  - Execute `scripts/seed-premium-cards.js` to populate 68 premium benefits
  - Verify all 87 benefits have claimingCadence populated
  - Verify no benefits with NULL claimingAmount
- **Acceptance Criteria**:
  - All 87 benefits seeded
  - Data matches research findings
  - No validation errors
  - Database ready for QA

---

### Feature 5.2: Testing & Validation

#### Story 5.2.1: Create comprehensive test suite for claiming utilities
- **File**: `src/lib/__tests__/claiming-validation.test.ts`
- **Changes**:
  - Unit tests for all 5 cadence types:
    - MONTHLY: Validate monthly boundaries, period reset, expiration
    - QUARTERLY: Validate quarter boundaries including Amex Sept 18 split
    - SEMI_ANNUAL: Validate half-year boundaries with custom window end
    - FLEXIBLE_ANNUAL: Validate full-year window
    - ONE_TIME: Validate single-use enforcement
  - Test edge cases:
    - Amex Platinum Sept 18 split (Q1-Q2 and H1-H2)
    - Varying monthly amounts (Uber $15/mo, $35 Dec)
    - Leap years
    - Period boundaries at midnight UTC
  - Test validation functions:
    - validateClaimingAmount() with various scenarios
    - isClaimingWindowOpen() for each cadence
    - daysUntilExpiration() calculations
- **Acceptance Criteria**:
  - Test coverage > 95% of claiming utilities
  - All edge cases covered
  - Tests pass consistently
  - CI/CD integration ready

#### Story 5.2.2: Create integration tests for API validation
- **File**: `src/app/api/benefits/usage/__tests__/validation.test.ts`
- **Changes**:
  - Test POST /api/benefits/usage with:
    - Valid claims within limit
    - Claims that exceed limit (should fail)
    - Claims for ONE_TIME after already used (should fail)
    - Period boundary edge cases
  - Test GET /api/benefits/usage with:
    - Cadence filter
    - Urgency filter
    - Pagination
  - Test /api/benefits/claiming-limits with:
    - All 5 cadence types
    - Various dates within periods
    - Period boundary dates
- **Acceptance Criteria**:
  - All integration tests pass
  - API correctly rejects invalid claims
  - Error responses are clear and actionable
  - Performance acceptable

#### Story 5.2.3: Create component test suite for UI components
- **File**: Multiple test files in components/__tests__/
- **Changes**:
  - BenefitUsageProgress.test.tsx:
    - Test rendering for each cadence type
    - Test urgency color coding
    - Test countdown display
  - MarkBenefitUsedModal.test.tsx:
    - Test amount validation
    - Test error messages
    - Test period window display
  - CadenceIndicator.test.tsx:
    - Test badge rendering
    - Test tooltip content
  - Use React Testing Library
  - Mock benefit data from research findings
- **Acceptance Criteria**:
  - Component tests pass
  - Coverage > 85%
  - Snapshots match reference
  - Accessibility tests pass

---

## Epic 6: Documentation & Deployment

### Feature 6.1: Documentation

#### Story 6.1.1: Create Phase 6C implementation guide
- **File**: `docs/PHASE6C-IMPLEMENTATION-GUIDE.md`
- **Changes**:
  - Overview of claiming cadences
  - Architecture diagram showing data flow
  - API endpoint reference
  - Component usage examples
  - Migration instructions
- **Acceptance Criteria**:
  - Clear, comprehensive documentation
  - Code examples for common use cases
  - API request/response examples

#### Story 6.1.2: Create frontend developer guide
- **File**: `docs/PHASE6C-FRONTEND-GUIDE.md`
- **Changes**:
  - How to use benefit claiming components
  - Cadence types and UI patterns
  - Theming and styling guidance
  - Integration with existing dashboard
- **Acceptance Criteria**:
  - Clear development instructions
  - Code snippets for each cadence type
  - TypeScript types documented

#### Story 6.1.3: Create data validation reference
- **File**: `docs/PHASE6C-VALIDATION-RULES.md`
- **Changes**:
  - Validation rules for each cadence type
  - Example scenarios (success and failure)
  - Error codes reference
- **Acceptance Criteria**:
  - All validation rules documented
  - Examples for each error case

---

### Feature 6.2: Deployment & Go-Live

#### Story 6.2.1: Create deployment checklist
- **File**: `PHASE6C-DEPLOYMENT-CHECKLIST.md`
- **Changes**:
  - Pre-deployment verification steps
  - Data migration steps
  - Rollback procedure
  - Post-deployment validation
  - Monitoring & alerting setup
- **Acceptance Criteria**:
  - Comprehensive checklist
  - Clear success criteria for each step
  - Rollback procedure documented

#### Story 6.2.2: Execute production deployment
- **Changes**:
  - Backup production database
  - Run migration against production
  - Seed production database with claiming data
  - Verify all features work in production
  - Enable monitoring for new fields
- **Acceptance Criteria**:
  - Production deployment successful
  - Zero data loss
  - All features working
  - Monitoring active

---

## File Mapping & Dependencies

```
PHASE 6C FILE DEPENDENCY GRAPH
================================

TIER 1 (Data Model - Foundation)
├── prisma/schema.prisma
│   └── Adds: claimingCadence, claimingAmount, claimingWindowEnd fields
│       Dependencies: None (foundation)
└── prisma/migrations/[timestamp]_add_claiming_cadence_fields/
    └── Depends on: schema.prisma (generates from schema)
    └── Creates: Database columns
    └── Used by: Next step (seeding)

TIER 2 (Seed Data - Population)
├── scripts/seed-premium-cards.js
│   └── Depends on: schema.prisma (for field types)
│   └── Populates: 68 premium card benefits
│   └── References: PHASE6C-CLAIMING-CADENCE-RESEARCH-FINDINGS.md
└── prisma/seed.ts
    └── Depends on: schema.prisma
    └── Populates: 19 master catalog benefits
    └── Runs: npx prisma db seed

TIER 3 (Type Definitions & Constants - Build Time)
├── src/lib/benefit-period-utils.ts
│   └── Adds: ClaimingCadence types, getClaimingWindowBoundaries(), getClaimingLimitForPeriod()
│   └── Depends on: Prisma schema (types)
│   └── Used by: All components and API routes
├── src/lib/claiming-cadence-constants.ts (NEW)
│   └── Exports: CLAIMING_CADENCES enum, cadence labels, urgency levels
│   └── Used by: Components, utilities, validation
└── src/lib/claiming-validation.ts (NEW)
    └── Exports: validateClaimingAmount(), isClaimingWindowOpen(), daysUntilExpiration()
    └── Depends on: benefit-period-utils.ts, Prisma types
    └── Used by: Components, API routes, tests

TIER 4 (Frontend Components - UI Layer)
├── src/components/benefits/BenefitUsageProgress.tsx (UPDATED)
│   ├── Depends on: claiming-cadence-constants.ts, claiming-validation.ts
│   ├── Uses: getClaimingLimitForPeriod(), daysUntilExpiration()
│   └── Renders: Cadence-specific UI with urgency indicators
├── src/components/benefits/MarkBenefitUsedModal.tsx (UPDATED)
│   ├── Depends on: claiming-validation.ts, benefit-period-utils.ts
│   ├── Uses: validateClaimingAmount(), getClaimingWindowBoundaries()
│   └── Validates: User claims against period limits
├── src/components/benefits/CadenceIndicator.tsx (NEW)
│   ├── Depends on: claiming-cadence-constants.ts
│   └── Renders: Reusable cadence badge with urgency
├── src/components/benefits/ClaimingLimitInfo.tsx (NEW)
│   ├── Depends on: claiming-validation.ts
│   └── Renders: Period limits and progress
└── src/components/benefits/PeriodClaimingHistory.tsx (NEW)
    ├── Depends on: benefit-period-utils.ts
    └── Renders: Historical claiming data with missed periods

TIER 5 (API Routes - Backend Validation)
├── src/app/api/benefits/usage/route.ts (UPDATED)
│   ├── Depends on: claiming-validation.ts, benefit-period-utils.ts
│   ├── Validates: Claiming limits on POST
│   ├── Enhances: GET response with claiming metadata
│   └── Database: Reads MasterBenefit.claimingCadence, .claimingAmount
└── src/app/api/benefits/claiming-limits/route.ts (NEW)
    ├── Depends on: claiming-validation.ts, benefit-period-utils.ts
    └── Provides: Real-time claiming limit calculations

TIER 6 (Tests - Validation)
├── src/lib/__tests__/claiming-validation.test.ts (NEW)
│   ├── Tests: All claiming utility functions
│   └── Depends on: claiming-validation.ts, benefit-period-utils.ts
├── src/app/api/benefits/usage/__tests__/validation.test.ts (NEW)
│   ├── Tests: API validation logic
│   └── Depends on: usage/route.ts
└── src/components/benefits/__tests__/*.test.tsx (NEW)
    ├── Tests: All new and updated components
    └── Depends on: Component files

TIER 7 (Documentation)
├── docs/PHASE6C-IMPLEMENTATION-GUIDE.md
├── docs/PHASE6C-FRONTEND-GUIDE.md
├── docs/PHASE6C-VALIDATION-RULES.md
├── docs/PHASE6C-BENEFIT-CLAIMING-MAPPING.md
└── PHASE6C-DEPLOYMENT-CHECKLIST.md

Cross-Dependencies:
═════════════════
- BenefitUsageProgress ←→ MarkBenefitUsedModal (shared benefit data)
- All components ← benefit-period-utils.ts (period calculations)
- All API routes ← claiming-validation.ts (validation logic)
- Components ← API routes (real-time limits via GET /claiming-limits)
- Tests ← All corresponding implementation files
```

---

## Parallelizable Work Streams

### Stream 1: Data Model & Schema (Days 1-2)
**DBA/Database Engineer**
- Story 1.1.1: Update schema.prisma
- Story 1.1.2: Create migration file
- Story 1.2.3: Create mapping documentation
- Story 5.1.1: Execute migration (dev only)

**Blocks**: All other work depends on this

### Stream 2: Utilities & Backend (Days 1-3, parallel with Stream 1)
**Backend SWE**
- Story 1.1.3: TypeScript type definitions
- Story 2.1.1: Update calculateAmountPerPeriod()
- Story 2.1.2: Create getClaimingWindowBoundaries()
- Story 2.1.3: Create getClaimingLimitForPeriod()
- Story 2.2.1: Create claiming validation module
- Story 4.1.1: Add validation to POST /api/benefits/usage
- Story 4.1.2: Enhance GET /api/benefits/usage
- Story 4.1.3: Create /api/benefits/claiming-limits endpoint
- Story 5.2.1: Create utility tests
- Story 5.2.2: Create API integration tests

**Blocks**: Frontend component development

### Stream 3: Frontend Components (Days 2-4, parallel with other streams after types available)
**Frontend SWE / UX Designer**
- Story 3.1.1: Update BenefitUsageProgress
- Story 3.1.2: Create CadenceIndicator
- Story 3.1.3: Create PeriodClaimingHistory
- Story 3.2.1: Update MarkBenefitUsedModal
- Story 3.2.2: Create ClaimingLimitInfo
- Story 5.2.3: Component tests
- Story 6.1.2: Frontend developer guide

**Depends on**: Stream 2 (types and validation available)

### Stream 4: Data Seeding (Days 2-3, parallel after schema)
**Data Engineer / SWE**
- Story 1.2.1: Map 68 premium benefits
- Story 1.2.2: Map 19 master benefits
- Story 5.1.2: Execute seeding

**Depends on**: Stream 1 (schema ready)

### Stream 5: Testing & QA (Days 4-5, parallel after implementation)
**QA Engineer**
- Story 5.2.1: Utility test suite
- Story 5.2.2: API integration tests
- Story 5.2.3: Component tests
- Create test scenarios for all edge cases

**Depends on**: All implementation streams

### Stream 6: Documentation & Deployment (Days 5-6, final)
**Tech Lead / DevOps**
- Story 6.1.1: Implementation guide
- Story 6.1.2: Frontend guide
- Story 6.1.3: Validation reference
- Story 6.2.1: Deployment checklist
- Story 6.2.2: Production deployment

**Depends on**: All implementation complete

---

## Critical Dependencies & Blocking Relationships

```
BLOCKING CHAIN (Must complete in order):
1. schema.prisma changes → migration creation → migration execution
   (Cannot seed without DB schema ready)

2. Type definitions → Backend utilities → API routes
   (Cannot write API logic without types)

3. Type definitions → Frontend components
   (Cannot write components without types)

4. Backend utilities + Type definitions → All validation logic
   (Validation depends on claiming utilities)

5. All implementation complete → Tests
   (Tests validate complete implementation)

6. All implementation + tests pass → Deployment
   (Cannot deploy with failing tests)

NON-BLOCKING PARALLEL WORK:
- Schema creation AND utility development (different layers)
- Backend utilities AND data seeding (independent)
- Frontend components AND backend routes (after types available)
- Documentation (can start early with drafts)
```

---

## Task Breakdown for Downstream Agents

### For PostgreSQL DBA
**Responsibilities**: Stories 1.1.2, 5.1.1

1. **Create Migration File** (Story 1.1.2)
   - Location: `prisma/migrations/20260407_add_claiming_cadence_fields/migration.sql`
   - Review schema.prisma for field definitions
   - Write SQL to add 3 new columns with appropriate types
   - Include rollback statements
   - Validate migration syntax

2. **Execute Dev Migration** (Story 5.1.1)
   - Back up development database
   - Run `prisma migrate dev`
   - Verify schema changes with `prisma studio` or `SELECT * FROM information_schema.columns`
   - Confirm all 3 new columns present
   - Regenerate Prisma client

**Success Criteria**:
- [ ] Migration file created and reviewed
- [ ] Schema changes apply cleanly
- [ ] No data loss in development
- [ ] Prisma types updated
- [ ] Zero migration errors

---

### For Backend SWE / Database Engineer
**Responsibilities**: Stories 1.1.3, 2.1.1-2.1.3, 2.2.1, 4.1.1-4.1.3, 5.2.1-5.2.2, 1.2.1-1.2.2

1. **Type Definitions** (Story 1.1.3)
   ```typescript
   // File: src/lib/benefit-period-utils.ts
   export type ClaimingCadence = 'MONTHLY' | 'QUARTERLY' | 'SEMI_ANNUAL' | 'FLEXIBLE_ANNUAL' | 'ONE_TIME';
   
   export interface ClaimingBenefit {
     claimingCadence: ClaimingCadence;
     claimingAmount: number; // in cents
     claimingWindowEnd?: string; // "0918" for Sept 18
   }
   
   // Type guards
   export function isValidClaimingCadence(value: any): value is ClaimingCadence {
     return ['MONTHLY', 'QUARTERLY', 'SEMI_ANNUAL', 'FLEXIBLE_ANNUAL', 'ONE_TIME'].includes(value);
   }
   ```

2. **Update calculateAmountPerPeriod()** (Story 2.1.1)
   - Current: Returns stickerValue or calculated amount
   - New: Fetch benefit.claimingAmount from DB
   - Handle all 5 cadence types
   - Test with research data examples

3. **Create getClaimingWindowBoundaries()** (Story 2.1.2)
   - Special handling for Sept 18 split
   - Test edge cases (leap years, month-end boundaries)
   - Return PeriodBoundaries { start, end }

4. **Create getClaimingLimitForPeriod()** (Story 2.1.3)
   - Input: benefit, periodDate, usageRecords
   - Output: remaining claimable amount
   - Handle ONE_TIME (return 0 after first use)

5. **Create claiming-validation.ts** (Story 2.2.1)
   - validateClaimingAmount()
   - isClaimingWindowOpen()
   - daysUntilExpiration()
   - Type-safe with full error handling

6. **Update POST /api/benefits/usage** (Story 4.1.1)
   - Fetch benefit with claimingCadence and claimingAmount
   - Validate against getClaimingLimitForPeriod()
   - Return detailed error if limit exceeded
   - Test: valid claim, over-claim, ONE_TIME after use

7. **Enhance GET /api/benefits/usage** (Story 4.1.2)
   - Add cadence metadata to response
   - Add ?cadence= and ?urgency= filters
   - Optimize query with indexes

8. **Create /api/benefits/claiming-limits endpoint** (Story 4.1.3)
   - GET endpoint returning real-time limits
   - Response includes: periodStart, periodEnd, maxClaimable, remaining, daysUntilExpiration
   - Performance: < 100ms

9. **Populate Premium Benefits** (Story 1.2.1)
   - File: scripts/seed-premium-cards.js
   - Reference: PHASE6C-CLAIMING-CADENCE-RESEARCH-FINDINGS.md
   - Add claimingCadence and claimingAmount to all 68 benefits
   - Special case: Amex Platinum with claimingWindowEnd: "0918"
   - Validate data before seeding

10. **Populate Master Benefits** (Story 1.2.2)
    - File: prisma/seed.ts
    - Add claiming values for 19 master benefits
    - Ensure consistency with premium seed

11. **Create Test Suite** (Story 5.2.1-5.2.2)
    - Unit tests for all utilities
    - Integration tests for API endpoints
    - Edge case tests (Amex Sept 18, leap years)
    - Coverage > 95%

**Deliverables**:
- [ ] TypeScript types properly defined and exported
- [ ] All utility functions implemented with 100% tests
- [ ] API routes enhanced and validated
- [ ] 87 benefits seeded with claiming data
- [ ] Integration tests passing

---

### For Frontend SWE / UX Designer
**Responsibilities**: Stories 3.1.1-3.1.3, 3.2.1-3.2.2, 5.2.3

1. **Update BenefitUsageProgress** (Story 3.1.1)
   - Accept claimingCadence, claimingAmount, claimingWindowEnd props
   - Render cadence-specific messaging:
     - MONTHLY: "⏰ $15 available THIS MONTH (29 days left!)"
     - QUARTERLY: "⏰ $75 available THIS QUARTER (18 days left!)"
     - SEMI_ANNUAL: "⏰ $200 available H1 (74 days left!)"
     - FLEXIBLE_ANNUAL: "✓ $300 available anytime"
     - ONE_TIME: "One-time credit: $109"
   - Color-coded urgency badges (🔴 > 🟠 > 🟡 > 🟢)
   - Use daysUntilExpiration() for countdown

2. **Create CadenceIndicator** (Story 3.1.2)
   - Reusable component for cadence badge
   - Props: benefit, referenceDate
   - Show cadence label and days remaining
   - Tooltip with full details
   - Mobile-responsive

3. **Create PeriodClaimingHistory** (Story 3.1.3)
   - Show historical periods (12 months/4 quarters/etc)
   - Highlight missed periods (visual loss indicator)
   - Display amounts claimed per period
   - Responsive grid layout

4. **Update MarkBenefitUsedModal** (Story 3.2.1)
   - Show current period window dates
   - Display max claimable for this period
   - Show how much already claimed
   - Prevent over-claiming with validation
   - Add warning badge for MONTHLY benefits
   - Max input field dynamically set

5. **Create ClaimingLimitInfo** (Story 3.2.2)
   - Subcomponent showing period limits
   - Props: benefit, currentPeriodUsed, remainingAmount, periodWindow
   - Visual progress bar
   - Warning if near/at limit

6. **Create Component Tests** (Story 5.2.3)
   - Test each cadence type rendering
   - Test urgency color coding
   - Test modal validation
   - Accessibility compliance (a11y)
   - Coverage > 85%

**Design Reference from Research**:
```
┌─────────────────────────────────────────┐
│ Uber Credits - Amex Platinum            │ (MONTHLY - RED/CRITICAL)
├─────────────────────────────────────────┤
│ ⏰ $15 available THIS MONTH             │
│ ├─ Expires: Mar 31, 2026 (3 days left!) │
│ └─ Monthly Limit: $15 max               │
│                                         │
│ [Claim Now]  [History]                  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Airline Fee Credit - Amex Platinum      │ (FLEXIBLE_ANNUAL - GREEN)
├─────────────────────────────────────────┤
│ ✓ $200 available anytime                │
│ ├─ Expires: Dec 31, 2026 (9 months)     │
│ └─ Flexible: No monthly/quarterly limit │
│                                         │
│ Progress: ▓▓░░░░░░░░ 40% ($80 used)     │
│ [Claim Now]                             │
└─────────────────────────────────────────┘
```

**Deliverables**:
- [ ] All 5 cadence types render correctly
- [ ] Urgency indicators display properly
- [ ] Modal validation prevents over-claiming
- [ ] Components responsive on mobile/tablet/desktop
- [ ] Accessibility tests passing
- [ ] Component tests > 85% coverage

---

### For QA / Test Engineer
**Responsibilities**: Stories 5.2.1-5.2.3, validation

1. **Test Claiming Utilities** (Story 5.2.1)
   - Create test file: `src/lib/__tests__/claiming-validation.test.ts`
   - Test cases for each cadence:
     - MONTHLY: Boundaries, expiration, reset
     - QUARTERLY: Q1-Q4, Amex Sept 18 split
     - SEMI_ANNUAL: H1/H2, Sept 18 handling
     - FLEXIBLE_ANNUAL: Full year window
     - ONE_TIME: Single-use enforcement
   - Edge cases:
     - Leap years (Feb 29)
     - Month-end boundaries (midnight UTC)
     - Amex Platinum special dates
     - Varying amounts (Uber $35 in Dec vs $15 other months)
   - Run: `npm test -- claiming-validation.test.ts`
   - Coverage requirement: > 95%

2. **Test API Endpoints** (Story 5.2.2)
   - Test POST /api/benefits/usage:
     - Valid claim within limit → Success
     - Claim > limit → CLAIMING_LIMIT_EXCEEDED error
     - ONE_TIME after use → ALREADY_CLAIMED error
     - Invalid benefit ID → 404 error
     - Missing required fields → 400 error
   - Test GET /api/benefits/usage:
     - Returns all usage records
     - ?cadence=MONTHLY filter works
     - ?urgency=CRITICAL filter works
     - Pagination works correctly
   - Test GET /api/benefits/claiming-limits:
     - Returns correct limits for benefit
     - Handles period boundary dates
     - Response includes all required fields
     - Performance < 100ms

3. **Test Frontend Components** (Story 5.2.3)
   - BenefitUsageProgress:
     - MONTHLY shows countdown badge
     - FLEXIBLE_ANNUAL shows simple progress
     - Urgency colors correct
     - Click to open modal
   - MarkBenefitUsedModal:
     - Max input limited by remaining amount
     - Shows period window dates
     - Prevents over-claiming
     - Error messages clear
   - CadenceIndicator:
     - Badge renders correctly
     - Tooltip shows full info
     - Mobile responsive
   - Use React Testing Library
   - Coverage > 85%

4. **Manual QA Test Scenarios** (From research findings)
   - Scenario 1: User misses March Uber credit
     - History shows: Mar: ✗ $15 (NOT CLAIMED - LOST)
     - System should warn user
   - Scenario 2: User tries to claim $20 in Uber (only $15 available)
     - Modal shows: "Monthly limit: $15 max"
     - Button disabled or shows error
   - Scenario 3: Amex Platinum Lululemon Q1 (Sept 18-Sept 30)
     - Show correct date window
     - Countdown shows 2 weeks remaining
     - Only $75 allowed this quarter
   - Scenario 4: Global Entry one-time credit
     - First use: Available to claim
     - After claim: Shows "Used on [date]"
     - Cannot claim again

**Test Data from Research**:
```
87 Benefits across 4 cards
- Amex Platinum: 12 benefits (6 MONTHLY/QUARTERLY/SEMI_ANNUAL, 4 FLEXIBLE, 2 ONE_TIME)
- Chase Sapphire Reserve: 4 benefits (all FLEXIBLE_ANNUAL + 1 ONE_TIME)
- Capital One Venture X: 3 benefits (all FLEXIBLE_ANNUAL)
- Delta SkyMiles Reserve: 2 benefits (1 FLEXIBLE, 1 ONE_TIME)
- Plus 66+ additional benefits across premium portfolio
```

**Deliverables**:
- [ ] Utility test suite with > 95% coverage
- [ ] API integration tests all passing
- [ ] Component tests with > 85% coverage
- [ ] Manual QA test scenarios documented and passed
- [ ] No TypeScript errors
- [ ] No console warnings in tests

---

### For DevOps / Deployment Engineer
**Responsibilities**: Stories 5.1.1, 6.2.1, 6.2.2

1. **Database Migration** (Story 5.1.1)
   - Development environment:
     - Backup dev database
     - Run `prisma migrate dev`
     - Verify schema changes
     - Regenerate Prisma client
     - Commit migration file
   - Staging environment (if applicable):
     - Backup staging database
     - Run migration
     - Verify changes
     - Test with production-like data

2. **Create Deployment Checklist** (Story 6.2.1)
   - File: `PHASE6C-DEPLOYMENT-CHECKLIST.md`
   - Pre-deployment verification:
     - [ ] All tests passing (unit, integration, component)
     - [ ] Code review completed
     - [ ] Database backup current
     - [ ] Zero open security issues
     - [ ] Performance benchmarks acceptable
   - Deployment steps:
     - [ ] Backup production database (snapshots)
     - [ ] Run migration: `prisma migrate deploy`
     - [ ] Verify schema: SELECT COUNT(*) FROM information_schema.columns WHERE table_name='MasterBenefit'
     - [ ] Seed production with claiming data (if needed)
     - [ ] Enable monitoring on new fields
     - [ ] Run smoke tests
   - Post-deployment validation:
     - [ ] All API endpoints responding
     - [ ] Claiming limits calculating correctly
     - [ ] UI renders without errors
     - [ ] No database errors in logs
     - [ ] Performance metrics normal
   - Rollback procedure (if needed):
     - [ ] Rollback migration: `prisma migrate resolve --rolled-back [migration-name]`
     - [ ] Restore from backup if data corruption
     - [ ] Verify rollback complete

3. **Production Deployment** (Story 6.2.2)
   - Execute pre-deployment checklist
   - Follow deployment steps:
     - Backup production database
     - Run migration
     - Seed production data (87 benefits with claiming cadence)
     - Verify in production environment
   - Enable alerting:
     - Monitor claimingAmount queries
     - Track /api/benefits/claiming-limits requests
     - Alert on claiming validation errors
   - Post-deployment communication:
     - Notify stakeholders of successful deployment
     - Share monitoring dashboard
     - Document any issues encountered

**Success Criteria**:
- [ ] Migration runs successfully
- [ ] Zero data loss
- [ ] All 87 benefits seeded with claiming data
- [ ] API endpoints responding
- [ ] Monitoring active

---

## Dependencies Documentation

### Critical Path Dependencies
```
1. schema.prisma (START HERE)
   ↓
2. migration file (generated from schema)
   ↓
3. Database migration executed (dev)
   ↓
4. Type definitions available
   ↓
5. Backend utilities written (parallel: data seeding)
   ↓
6. API routes updated (parallel: components written after types)
   ↓
7. All tests passing
   ↓
8. Production deployment
```

### File Update Sequence
```
MUST HAPPEN FIRST (Foundation):
1. prisma/schema.prisma
2. prisma/migrations/[timestamp]_add_claiming_cadence_fields/

THEN (In parallel):
2a. src/lib/benefit-period-utils.ts (types)
2b. scripts/seed-premium-cards.js (data)
2c. prisma/seed.ts (data)

THEN:
3. src/lib/claiming-cadence-constants.ts
4. src/lib/claiming-validation.ts

THEN (In parallel):
4a. src/app/api/benefits/usage/route.ts
4b. src/components/benefits/*.tsx

FINALLY:
5. All test files
6. Documentation
```

---

## Success Metrics

### Data Model
- ✅ All 3 new fields present in MasterBenefit
- ✅ Migration runs cleanly on dev/staging/prod
- ✅ All 87 benefits have claimingCadence populated
- ✅ All 87 benefits have claimingAmount in cents
- ✅ Zero NULL values in claimingCadence

### API Validation
- ✅ POST /api/benefits/usage validates against claimingAmount
- ✅ Rejects claims exceeding limit with clear error
- ✅ GET /api/benefits/usage includes claiming metadata
- ✅ GET /api/benefits/claiming-limits returns < 100ms
- ✅ All 5 cadence types validated correctly

### Frontend UI
- ✅ All 5 cadence types render with appropriate messaging
- ✅ MONTHLY benefits show urgent countdown badges
- ✅ FLEXIBLE_ANNUAL benefits show simple progress
- ✅ Components responsive on mobile/tablet/desktop
- ✅ Urgency colors display correctly
- ✅ Modal prevents over-claiming

### Testing
- ✅ Utility tests > 95% coverage
- ✅ API integration tests all passing
- ✅ Component tests > 85% coverage
- ✅ Zero TypeScript errors
- ✅ All manual QA scenarios pass

### Deployment
- ✅ Production migration successful
- ✅ Zero data loss
- ✅ All features working in production
- ✅ Monitoring active and alerting

---

## Timeline Estimate

| Phase | Days | Tasks | Owner |
|-------|------|-------|-------|
| Planning | 1 | Review research, create plan | Tech Lead |
| Schema | 1 | Schema.prisma, migration file | DBA |
| Utilities & API | 2 | Types, validation, endpoints | Backend SWE |
| Data Seeding | 1 | Populate 87 benefits | Data Engineer |
| Frontend | 2 | Components and modal | Frontend SWE |
| Testing | 1-2 | Unit, integration, component tests | QA Engineer |
| Documentation | 1 | Guides, references, checklists | Tech Lead |
| Deployment | 1 | Dev → Staging → Production | DevOps |
| **Total** | **9-10 days** | **18 stories** | **Cross-functional team** |

---

## Risk Mitigation

### Risk 1: Amex Sept 18 Split Complexity
- **Mitigation**: Create comprehensive test cases for Sept 18 boundaries
- **Owner**: Backend SWE (testing), QA Engineer (validation)
- **Test**: Multiple dates across Sept 18 (Sept 17, Sept 18, Sept 19)

### Risk 2: Monthly Benefit Loss Tracking Accuracy
- **Mitigation**: Create detailed PeriodClaimingHistory component with visual indicators
- **Owner**: Frontend SWE
- **Validation**: QA Engineer confirms lost periods highlighted correctly

### Risk 3: Database Migration on Production
- **Mitigation**: Comprehensive backup strategy, dry-run on staging first
- **Owner**: DevOps Engineer
- **Verification**: Full schema audit post-migration

### Risk 4: Claiming Limit API Performance
- **Mitigation**: Implement caching and index optimization
- **Owner**: Backend SWE
- **Target**: < 100ms response time

### Risk 5: UI/UX Consistency Across Cadence Types
- **Mitigation**: Create design system components (CadenceIndicator, ClaimingLimitInfo)
- **Owner**: Frontend SWE, UX Designer
- **Validation**: Component library ready for reuse

---

## Communication Plan

### Stakeholders
- **Product**: Benefits tracking feature, user education on cadences
- **Engineering**: Schema review, API design, component architecture
- **QA**: Test plan review, edge case identification
- **DevOps**: Deployment strategy, monitoring setup

### Milestones
1. ✅ Research complete → Implementation planning (Now)
2. 🔄 Schema ready → Backend & data work begins
3. 🔄 Utilities ready → Frontend components begin
4. 🔄 All tests passing → Deployment ready
5. 🔄 Production live → Monitoring active

---

## Next Steps

1. **Review this plan** with engineering team
2. **Assign story owners** based on expertise and capacity
3. **Start Story 1.1.1** (schema.prisma updates) immediately
4. **Establish daily standup** for coordination
5. **Create tracking board** with story status
6. **Set up monitoring** for deployment validation

---

This comprehensive plan provides clear direction for all 18 stories across 3 parallel work streams, with specific deliverables, success criteria, and dependencies documented for downstream agents. The plan is designed to be executed in 9-10 days with a cross-functional team.